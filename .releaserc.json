{
  "branches": ["main", "master"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    ["@semantic-release/npm", {
      "npmPublish": false
    }],
    ["@semantic-release/exec", {
      "prepareCmd": "cd client && npm version ${nextRelease.version} --no-git-tag-version --allow-same-version && cd ../electron && npm version ${nextRelease.version} --no-git-tag-version --allow-same-version"
    }],
    ["@semantic-release/git", {
      "assets": ["package.json", "client/package.json", "electron/package.json", "CHANGELOG.md"],
      "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    "@semantic-release/github"
  ]
}
