version: '3.8'

services:
  backend:
    build: ./server
    ports:
      - "3001:3001"
    volumes:
      - ./server/repos:/app/repos

  frontend:
    build: ./client
    ports:
      - "8080:80"
    depends_on:
      - backend
